import 'package:flutter/material.dart';import 'package:provider/provider.dart';import 'package:myapp/core/providers/auth_provider.dart';
class ProfileScreen extends StatelessWidget{const ProfileScreen({super.key});@override Widget build(BuildContext context){final authProvider=context.watch<AuthProvider>();final userProfile=authProvider.userProfile;if(userProfile==null){return Scaffold(appBar:AppBar(title:const Text('Meu Perfil')),body:const Center(child:CircularProgressIndicator()));}return Scaffold(appBar:AppBar(title:const Text('Meu Perfil')),body:SingleChildScrollView(padding:const EdgeInsets.all(24.0),child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[Center(child:CircleAvatar(radius:60,backgroundColor:Theme.of(context).primaryColor,child:Text(userProfile.nickname.isNotEmpty?userProfile.nickname[0].toUpperCase():'?',style:const TextStyle(fontSize:48,fontWeight:FontWeight.bold,color:Colors.white)))),const SizedBox(height:24),Center(child:Text(userProfile.nickname,style:Theme.of(context).textTheme.headlineSmall)),const SizedBox(height:8),Center(child:Text('Nível ${userProfile.level} | ${userProfile.nutriScore} XP',style:Theme.of(context).textTheme.titleMedium?.copyWith(color:Colors.white70))),const Divider(height:40,color:Colors.white24),Text('Funcionalidades de edição em breve!',style:Theme.of(context).textTheme.bodyLarge,textAlign:TextAlign.center)])));}}
